# Environment configuration for Vera AI Bot
PORT=8080
HOST=0.0.0.0
ENVIRONMENT=production

# Optional: LLM provider overrides for frontier model generation
# Default: High-performance deterministic grounded ensemble (0ms latency, zero hallucinations)
# Supported: gemini, openai, anthropic, deepseek, groq
LLM_PROVIDER=
LLM_API_KEY=
LLM_MODEL=
